# 任务拆解与交付边界

## 1. 数据基础

- [x] 独立 SQLite、事务、消息幂等、稳定用户 ID、强制群隔离。
- [x] 记忆与原文引用、修改版本、冲突保护、停用和删除。
- [x] 后台待处理记录和向量 outbox 持久化，重启恢复。

## 2. 自动记忆

- [x] 有界批处理、噪声过滤、来源 ID/逐字引用校验、转述待审。
- [x] 中文关键词与可选向量检索，原文回查与核验，允许零注入。
- [x] 日调用预算、超时、并发控制、失败降级、trace；v0.1.2 移除影子模式，只保留启用／停用。

## 3. 管理服务

- [x] AstrBot Pages 原生登录授权，无独立监听端口或第二套 Token 登录。
- [x] 固定操作路由白名单、分页、请求封装大小与输入校验。
- [x] 管理 API、模型 ID 列表、只读试召回、人物删除。

## 4. React + shadcn

- [x] 复用 AstrBot 登录、记忆检索、来源详情、版本、编辑/停用/删除。
- [x] 召回记录、实验室、策略/预算/作用域配置。
- [x] 响应式界面、构建产物、组件源码及锁文件。

## 5. AstrBot 接入

- [x] 群消息钩子、请求前尾部追加并保留历史、启动/停用/重载资源管理。
- [x] Pages 直接加载深色管理界面，通过 Bridge 调用插件管理 API。
- [x] 真实 AstrBot 源码环境集成检查脚本。

## 6. 交付

- [x] 单元/API/浏览器测试、CI、安装说明、安全边界。
- [ ] 用户审核后提交、创建远端仓库和推送（本轮不执行）。
- [x] 用户批准后在 AstrBot 4.28.1 安装，一个指定 QQ 群启用影子运行；配置、采集、面板鉴权与热重载已验证。
- [ ] 真实语料的召回精度/错误归属/延迟评估；不把合成测试当作效果证明。

## 有意留待后续

飞书适配、自动处理撤回事件、原文批量历史导入、全文图像理解、跨群实体合并、分角色管理权限、在线模型成本金额统计。

首版对不明确的来源保守丢弃，对无来源记忆不能启用。它不是一个已证明不会误记的系统；应从小范围启用并持续检查来源和召回记录。
