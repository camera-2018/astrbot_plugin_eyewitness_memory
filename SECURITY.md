# 安全说明

- 管理页面和 API 复用 AstrBot 登录与插件页面授权，不单独监听网络端口。公网/不可信链路需为 AstrBot 配置 HTTPS 与网络访问控制。
- 不再生成或读取 `panel.token`；旧文件失效。开发测试服务器的固定令牌仅用于隔离合成环境。
- 聊天是模型输入中的不可信数据。程序固定作用域、验证来源引用，但无法保证模型永不受提示注入影响或永不误解玩笑。应定期检查召回记录及原文。
- 原文脱敏为尽力而为的模式匹配，不是完整 DLP。只开启得到授权的群，并配置保留策略。
- 主机文件权限不能代替 Docker 卷、主机账户和备份安全。SQLite 含明文群聊内容，不做应用层静态加密。
- 常规删除不等于取证级物理擦除；SQLite 空闲页、WAL、磁盘快照或外部备份可能保留旧内容。永久敏感数据清理需结合停机维护和备份管理。
- 删除记忆会排队清理向量，清理期间旧向量无法通过本地记忆存在性/状态校验。已不再配置的外部 Qdrant 服务需管理员清理。
- 为保持跨轮追加，已注入原文片段会保存到 AstrBot 会话历史；删除插件记忆或人物数据不会回改这些历史，也不会清除提供方日志/缓存和备份。需要彻底删除时应另外删除对应会话并处理外部副本。
- 局部上下文可能包含不同人的插话，只是待解读的聊天数据，不是系统指令或事实背书。旧记录只能标为采集时间，不能当作精确发送时间。
- 管理凭据轮换和退出由 AstrBot 处理；不要把开发测试 HTTP 服务作为生产入口。
- 报告问题时只提供最小匿名化复现，不附真实聊天库、令牌、Cookie 或模型 Key。
